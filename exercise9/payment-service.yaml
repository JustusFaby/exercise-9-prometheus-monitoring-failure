apiVersion: v1
kind: Service
metadata:
  name: payment-service
  labels:
    app: payment
spec:
  selector:
    app: payment
  ports:
  - name: prometheus
    port: 9100
    targetPort: 9100