apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: payment-service
  namespace: monitoring
spec:
  selector:
    matchLabels:
      app: payment

  namespaceSelector:
    any: true

  endpoints:
  - port: metrics